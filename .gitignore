bin/
obj/
*.user
.vs/
artifacts/
cbom-out/
.cbom/out/
TestResults/
*.binlog

# Generated by scripts/generate-tool-sbom.sh and by release.yml, which attaches it to the GitHub Release.
# Tracking it means every release leaves a stale copy in the tree; the release asset is the source of truth.
sbom/
